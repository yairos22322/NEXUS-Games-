"""Cinematic rendering systems for NEXUS FIVE 3D.

The package deliberately keeps its public surface small.  The application
creates one :class:`GraphicsDirector`, and every game mode receives the same
rendering upgrades through the shared scene graph.
"""
